#include <cs50.h>
#include <stdio.h>

long long fact(long long num);

int main(void)
{
   fact(0);
}

long long fact(long long num)
{
    //long long product;

    if (num == 0)
    {
        printf("1\n");
    }

    return 0;
}